import type { Meta } from '@storybook/react';

import { Login } from './Login';

const meta = {
  title: 'Designsystem / UI Components / Login',
  component: Login,
} satisfies Meta<typeof Login>;

export default meta;

export const Default = {};
