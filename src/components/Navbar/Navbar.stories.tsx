import type { Meta } from '@storybook/react';

import { Navbar } from './Navbar';

const meta = {
  title: 'Designsystem / UI Components / Navbar',
  component: Navbar,
} satisfies Meta<typeof Navbar>;

export default meta;

export const Default = {};
